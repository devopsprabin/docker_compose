version: '3.3'
services:
  mysql:
    image: mysql
    restart: always
    environment:
      MYSQL_DATABASE: 'anydone'
      MYSQL_USER: 'anydone'
      MYSQL_PASSWORD: 'nb3?N#tq'
      # Password for root access
      MYSQL_ROOT_PASSWORD: 'root'
    ports:
      - '3306:3306'
    expose:
      # Opens port 3306 on the container
      - '3306'
      # Where our data will be persisted
    volumes:
      - data:/var/lib/mysql
      - ./anydone.sql:/home/anydone.sql
      - ./restore.sh:/home/restore.sh
      - ./export.query.sql:/home/export.query.sh
# Names our volume
volumes:
  data: